connection "confluence" {
    plugin    = "ellisvalentiner/confluence"

    # Base URI of your Confluence Cloud instance
    base_url = "{{ (onepasswordItemFields "2zocp4xayjb5nksccd6vs4lrmq" "3rs5ui53xhp5zfe63vltdpb6o4" "vendhq").url.value }}"

    # The user name to access the Confluence cloud instance
    username = "{{ .work.email }}"

    # Access Token for the API
    # See https://id.atlassian.com/manage/api-tokens
    token = "{{ (onepasswordDetailsFields "2zocp4xayjb5nksccd6vs4lrmq" "3rs5ui53xhp5zfe63vltdpb6o4" "vendhq").credential.value }}"
}
